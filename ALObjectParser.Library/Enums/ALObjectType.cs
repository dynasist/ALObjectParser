using System;
using System.Collections.Generic;
using System.Text;

namespace ALObjectParser
{
    public enum ALObjectType
    {
        table,
        tableextension,
        page,
        pagecustomization,
        pageextension,
        report, 
        codeunit,
        xmlport,
        query,
        controladdin,
        @enum,
        dotnet,
        profile,
        enumextension,
        @interface
    }
}
